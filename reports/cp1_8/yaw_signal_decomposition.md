# CP1.8 yaw signal decomposition

CP1.7 stored aggregate metrics but not 200 Hz yaw/contact/action telemetry. The decomposition is therefore explicitly blocked, not inferred from aggregate RMSE. No filtered metric is substituted for the strict raw gate.
